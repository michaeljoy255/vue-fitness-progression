import _RecordContainer from './_RecordContainer.js'

/**
 * 
 */
export default class MeasurementRecordContainer extends _RecordContainer {
  constructor({ items = [] } = {}) {
    super({ items })
  }
}
